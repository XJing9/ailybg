<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.dao.PhotoDao">
    <!--<delete id="pldel" parameterType="java.util.List">
        DELETE from photo where pho_id in
        <foreach collection="list" item="pho_id"
                 open="(" close=")" index="index" separator=",">
            #{pho_id}
        </foreach>
    </delete>-->
</mapper>